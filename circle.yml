machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t mental/postgresql .
    - docker run -d --name=postgresql mental/postgresql; sleep 10
    - docker run -it --volumes-from=postgresql mental/postgresql sudo -u postgres -H psql -c "\conninfo"
